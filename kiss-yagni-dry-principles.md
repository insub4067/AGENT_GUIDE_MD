---
title: KISS · YAGNI · DRY — 핵심 개발 원칙
purpose: AI 에이전트가 불필요한 복잡도·기능·중복을 만들지 않도록 구현 판단 기준을 제공한다
scope: 언어·프레임워크 무관
updated: 2026-08
---

# KISS · YAGNI · DRY

> **필요한 것만 만들고(YAGNI), 가장 단순한 방법으로 만들며(KISS), 같은 지식은 한 곳에서 관리한다(DRY).**

세 원칙은 각각 따로 적용하는 규칙이 아니라 하나의 구현 판단 흐름으로 사용한다.

1. **YAGNI — 이걸 지금 만들어야 하는가?**
2. **KISS — 만든다면 더 단순하게 만들 수 없는가?**
3. **DRY — 같은 지식이나 정책이 이미 다른 곳에 존재하지 않는가?**

---

## 1. KISS — Keep It Simple, Stupid

### 핵심
현재 요구사항을 만족하는 방법 중 **가장 이해하기 쉬운 해결책**을 선택한다.

단순함은 코드 줄 수가 적다는 뜻이 아니다. 읽는 사람이 알아야 하는 개념, 계층, 규칙, 예외가 적다는 뜻이다.

### 에이전트 규칙
- 같은 문제를 직접적인 코드로 풀 수 있다면 불필요한 패턴·레이어·프레임워크를 추가하지 않는다.
- 영리한 한 줄 코드보다 의도가 드러나는 평범한 코드를 우선한다.
- 단순한 `if`, 함수, 자료구조로 충분한 문제에 전략 패턴·팩토리·추상 클래스 등을 먼저 도입하지 않는다.
- 성능 최적화는 측정 결과가 있을 때만 한다. 가독성을 희생하는 사전 최적화를 하지 않는다.
- 기존 코드베이스가 이미 사용하는 단순한 관례가 있다면 새로운 방식보다 그것을 따른다.

### 위반 신호
- 기능 하나를 이해하려면 여러 계층을 따라가야 한다.
- 구현보다 설정과 추상화 코드가 더 많다.
- "나중에 확장하기 좋게"라는 이유로 현재 필요하지 않은 구조가 생긴다.
- 같은 동작을 더 적은 개념으로 설명할 수 있는데 복잡한 패턴을 사용한다.

### 판정 질문
> **이 동작을 유지하면서 개념·계층·분기를 하나라도 제거할 수 있는가?**

가능하다면 더 단순한 쪽을 우선한다.

---

## 2. YAGNI — You Ain't Gonna Need It

### 핵심
**현재 확인된 요구사항에 없는 기능은 만들지 않는다.**

미래를 고려한다는 이유로 미래 기능을 미리 구현하지 않는다. 미래 변경을 쉽게 만드는 가장 좋은 방법은 지금 코드를 작고 명확하게 유지하는 것이다.

### 에이전트 규칙
- 요청되지 않은 기능, 옵션, 설정값, 플러그인 포인트를 추가하지 않는다.
- 구현체가 하나뿐인데 미래 구현체를 예상해서 인터페이스를 만들지 않는다.
- 아직 존재하지 않는 요구사항을 위한 전략 패턴, 범용 프레임워크, 확장 훅을 만들지 않는다.
- "언젠가 필요할 수 있음"은 구현 근거가 아니다.
- 현재 호출되지 않는 코드나 설정을 남기지 않는다.
- 미래 요구가 실제로 등장하면 그때 현재 구조를 기준으로 리팩터링한다.

### 위반 신호
- 사용처가 없는 메서드·타입·옵션이 있다.
- 실제 요구사항에는 하나의 케이스만 있는데 여러 케이스를 미리 구현한다.
- 사용자 요청보다 변경 범위가 커진다.
- 코드 설명에 "혹시", "나중에", "미래에"가 구현 이유로 등장한다.

### 판정 질문
> **이 코드를 지금 실제로 호출하는 곳과, 이것을 요구한 현재 요구사항은 무엇인가?**

둘 중 하나라도 답할 수 없다면 추가하지 않는 것이 기본값이다.

---

## 3. DRY — Don't Repeat Yourself

### 핵심
DRY의 대상은 단순히 **똑같이 생긴 코드**가 아니라 **같은 지식·정책·의사결정의 중복**이다.

하나의 비즈니스 규칙이 여러 곳에 복제되면 규칙이 바뀔 때 모두 수정해야 하고, 일부만 수정되면 서로 다른 진실이 생긴다. 같은 지식은 하나의 권위 있는 위치에서 관리한다.

### 에이전트 규칙
- 동일한 비즈니스 규칙, 계산식, 정책 상수가 여러 곳에 존재하지 않게 한다.
- 기존 함수·타입·유틸이 있는지 먼저 검색한 뒤 새로 만든다.
- 복사·붙여넣기로 같은 정책을 확장하지 않는다.
- 두 코드의 모양이 비슷하다는 이유만으로 합치지 않는다.
- 서로 다른 이유로 변경될 코드라면 중복처럼 보여도 성급하게 공통화하지 않는다.
- 공통 의미가 확인되지 않았다면 추상화보다 잠시 중복을 허용한다. 잘못된 추상화는 단순 중복보다 제거하기 어렵다.

### 위반 신호
- 하나의 정책 변경 때문에 여러 파일을 함께 수정해야 한다.
- 같은 상수·검증식·계산 규칙이 여러 위치에 복사돼 있다.
- 어느 구현이 최신인지 판단하기 어렵다.
- 공통화한 함수 안에 다시 타입별 `if`/`switch`가 계속 늘어난다.

### 판정 질문
> **하나의 정책이 바뀌었을 때 이 코드를 두 곳 이상 수정해야 하는가?**

그렇다면 같은 지식이 중복됐을 가능성이 높다.

---

## 4. 세 원칙이 충돌할 때

### YAGNI vs 확장성
확장 가능한 구조와 미래 기능 선구현은 다르다.

- 현재 코드의 결합도를 낮추고 책임을 명확히 하는 것은 허용한다.
- 아직 존재하지 않는 기능의 구현체·옵션·분기까지 만드는 것은 금지한다.

### KISS vs DRY
중복을 없애기 위해 더 큰 추상화가 필요하다면 바로 합치지 않는다.

- **같은 지식이 반복되는 것이 확실하면 DRY를 적용한다.**
- 단지 코드 모양만 비슷하다면 KISS를 우선하고 각각 명확하게 둔다.
- 공통화한 결과 호출부보다 추상화가 더 이해하기 어려워지면 되돌린다.

### 기본 우선순위
1. 현재 요구사항과 올바른 동작
2. 명확성·가독성
3. YAGNI — 불필요한 것을 만들지 않기
4. KISS — 필요한 것을 단순하게 만들기
5. DRY — 실제로 같은 지식만 한 곳으로 모으기

DRY를 지키기 위해 YAGNI와 KISS를 깨지 않는다.

---

## 5. AI 에이전트 구현 전 체크

코드를 추가하기 전에 아래 세 문장에 답한다.

- **YAGNI:** 이 변경은 현재 어떤 요구사항 때문에 필요한가?
- **KISS:** 같은 요구사항을 더 적은 개념으로 구현할 수 있는가?
- **DRY:** 이미 같은 지식이나 정책을 표현하는 코드가 존재하는가?

코드를 작성한 뒤 다시 확인한다.

- 사용되지 않는 기능·옵션·추상화가 생기지 않았는가?
- 제거해도 동작이 동일한 계층이나 개념이 없는가?
- 하나의 정책 변경이 여러 파일 수정을 요구하지 않는가?
- 중복 제거를 명분으로 서로 다른 책임을 억지로 결합하지 않았는가?

---

## 6. 한 줄 적용 예시

```text
요청: 이메일 로그인 기능 추가

YAGNI  → 요청되지 않은 소셜 로그인 전략까지 만들지 않는다.
KISS   → 현재 한 가지 로그인 흐름이면 직접적인 함수/타입으로 구현한다.
DRY    → 이메일 검증 규칙이 이미 존재하면 새로 복사하지 않고 기존 규칙을 사용한다.
```

---

## 7. 참고

- 원문 참고: [알고 있으면 좋은 개발원칙 — KISS, YAGNI, DRY](https://blog.naver.com/finway/221163007357)
- 이 문서는 원문의 세 원칙을 그대로 복제하지 않고, `AGENT_GUIDE_MD`에서 AI 에이전트가 실제 코드 생성·수정 시 사용할 수 있도록 실행 규칙 중심으로 재구성했다.
- `development-guidelines.md`의 단순 설계, 복잡도, 추상화 관련 규칙과 함께 적용한다.
